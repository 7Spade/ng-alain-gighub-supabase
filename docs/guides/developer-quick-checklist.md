# 開發者快速檢查清單

## 📑 目錄

- [📋 環境檢查（5 分鐘）](#-環境檢查5-分鐘)
  - [✅ 必備工具檢查](#-必備工具檢查)
  - [✅ 專案依賴檢查](#-專案依賴檢查)
- [🔧 程式碼品質檢查（2 分鐘）](#-程式碼品質檢查2-分鐘)
  - [✅ Lint 檢查](#-lint-檢查)
  - [✅ 型別檢查](#-型別檢查)
- [🏗️ 建置檢查（3-5 分鐘）](#-建置檢查3-5-分鐘)
  - [✅ 開發建置](#-開發建置)
  - [✅ 生產建置（可選）](#-生產建置可選)
- [🚀 啟動開發伺服器（1 分鐘）](#-啟動開發伺服器1-分鐘)
  - [✅ 標準啟動](#-標準啟動)
- [🗄️ Supabase 連線檢查（3 分鐘）](#-supabase-連線檢查3-分鐘)
  - [✅ 環境變數檢查](#-環境變數檢查)
  - [✅ 連線測試](#-連線測試)
- [✅ Git 工作流程檢查](#-git-工作流程檢查)
  - [✅ 分支檢查](#-分支檢查)
  - [✅ Pre-commit Hooks](#-pre-commit-hooks)
- [🧪 測試檢查（可選）](#-測試檢查可選)
  - [✅ 單元測試](#-單元測試)
- [📚 文檔檢查](#-文檔檢查)
  - [✅ 核心文檔閱讀](#-核心文檔閱讀)
- [⚡ 快速驗證腳本](#-快速驗證腳本)
- [🚨 常見問題快速修復](#-常見問題快速修復)
  - [問題 1：`yarn install` 失敗](#問題-1yarn-install-失敗)
  - [問題 2：`yarn start` 失敗（端口被占用）](#問題-2yarn-start-失敗端口被占用)
  - [問題 3：TypeScript 編譯錯誤](#問題-3typescript-編譯錯誤)
  - [問題 4：Supabase 連線失敗](#問題-4supabase-連線失敗)
- [📝 開發前檢查表](#-開發前檢查表)
- [🎯 開發時持續檢查](#-開發時持續檢查)
  - [每次 Commit 前](#每次-commit-前)
  - [每次 Push 前](#每次-push-前)
- [📞 取得協助](#-取得協助)
- [🔗 相關文檔](#-相關文檔)

---


> ⚡ **目的**：為開發者提供快速檢查清單，確保開發環境正確設定並ready to code

**最後更新**：2025-01-20
**維護者**：開發團隊

- --

## 📋 環境檢查（5 分鐘）

### ✅ 必備工具檢查

```bash
# 1. 檢查 Node.js 版本（需要 >= 20）
node --version
# 預期輸出：v20.x.x 或更高

# 2. 檢查 Yarn 版本（需要 4.9.2+）
yarn --version
# 預期輸出：4.9.2 或更高

# 3. 檢查 Git
git --version
# 預期輸出：git version 2.x.x
```

### ✅ 專案依賴檢查

```bash
# 4. 安裝依賴（首次或 package.json 變更後）
yarn install

# 5. 檢查 Angular CLI
yarn ng version
# 應顯示 Angular CLI: 20.3.x
```

- --

## 🔧 程式碼品質檢查（2 分鐘）

### ✅ Lint 檢查

```bash
# 執行完整 lint 檢查
yarn lint

# 自動修復可修復的問題
yarn lint:ts --fix

# 檢查樣式
yarn lint:style
```

**預期結果**：
- ✅ TypeScript 錯誤：0 個
- ⚠️ TypeScript 警告：約 52 個（非阻塞）
- ⚠️ 樣式問題：約 139 個（非阻塞）

### ✅ 型別檢查

```bash
# TypeScript 編譯檢查
yarn ng build --configuration=development --no-output-hashing

# 或只做型別檢查（不建置）
yarn tsc --noEmit
```

- --

## 🏗️ 建置檢查（3-5 分鐘）

### ✅ 開發建置

```bash
# 建置開發版本
yarn build

# 建置成功標誌
# ✔ Browser application bundle generation complete.
# ✔ Copying assets complete.
# ✔ Index html generation complete.
```

### ✅ 生產建置（可選）

```bash
# 建置生產版本（較慢，約 5-10 分鐘）
yarn ng build --configuration=production
```

- --

## 🚀 啟動開發伺服器（1 分鐘）

### ✅ 標準啟動

```bash
# 啟動開發伺服器（自動開啟瀏覽器）
yarn start

# 或使用 HMR（熱模組替換，開發體驗更好）
yarn hmr
```

**驗證清單**：
- [ ] 瀏覽器自動開啟 http://localhost:4200
- [ ] 應用程式正常載入
- [ ] 無 Console 錯誤
- [ ] 可以正常導航

- --

## 🗄️ Supabase 連線檢查（3 分鐘）

### ✅ 環境變數檢查

檢查 `src/environments/environment.ts`：

```typescript
export const environment = {
  production: false,
  supabase: {
    url: 'https://your-project.supabase.co',  // ← 確認已填入
    anonKey: 'your-anon-key',                 // ← 確認已填入
    storage: {
      documentBucket: 'blueprint-documents'
    }
  }
};
```

### ✅ 連線測試

```bash
# 使用 Supabase MCP 工具測試連線
# 列出資料庫表
@SUPABASE 列出所有資料庫表

# 預期：顯示 51 張表的清單
```

- --

## ✅ Git 工作流程檢查

### ✅ 分支檢查

```bash
# 確認當前分支
git branch --show-current

# 確認遠端設定
git remote -v
```

### ✅ Pre-commit Hooks

```bash
# 確認 Husky 已安裝
ls -la .husky/

# 應該看到：
# .husky/pre-commit
# .husky/_/*
```

- --

## 🧪 測試檢查（可選）

### ✅ 單元測試

```bash
# 執行單元測試（交互模式）
yarn test

# 執行測試並產生覆蓋率報告
yarn test-coverage
```

- --

## 📚 文檔檢查

### ✅ 核心文檔閱讀

開始開發前，建議先閱讀：

1. ✅ [快速開始指南](./guides/getting-started.md)
2. ✅ [開發最佳實踐指南](./guides/development-best-practices.md)
3. ✅ [SHARED_IMPORTS 使用指南](./reference/shared-imports-guide.md)
4. ✅ [完整SQL表結構定義](./reference/sql-schema-definition.md)
5. ✅ [完整架構流程圖](./architecture/20-complete-architecture-flowchart.mermaid.md)

- --

## ⚡ 快速驗證腳本

複製以下腳本並在專案根目錄執行：

```bash
#!/bin/bash
# 快速驗證腳本 - quick-check.sh

echo "🔍 開始快速檢查..."
echo ""

# 1. Node.js 檢查
echo "1️⃣ 檢查 Node.js 版本..."
NODE_VERSION=$(node --version)
echo "   Node.js: $NODE_VERSION"

# 2. Yarn 檢查
echo "2️⃣ 檢查 Yarn 版本..."
YARN_VERSION=$(yarn --version)
echo "   Yarn: $YARN_VERSION"

# 3. 依賴檢查
echo "3️⃣ 檢查 node_modules..."
if [ -d "node_modules" ]; then
    echo "   ✅ node_modules 存在"
else
    echo "   ❌ node_modules 不存在，請執行 yarn install"
fi

# 4. Lint 檢查
echo "4️⃣ 執行 Lint 檢查..."
yarn lint --quiet 2>&1 | grep -E "✖|problem" | tail -1

# 5. 環境檔案檢查
echo "5️⃣ 檢查環境設定檔..."
if [ -f "src/environments/environment.ts" ]; then
    echo "   ✅ environment.ts 存在"
else
    echo "   ❌ environment.ts 不存在"
fi

echo ""
echo "✅ 檢查完成！"
```

保存為 `scripts/quick-check.sh` 並執行：

```bash
chmod +x scripts/quick-check.sh
./scripts/quick-check.sh
```

- --

## 🚨 常見問題快速修復

### 問題 1：`yarn install` 失敗

```bash
# 解決方案 1：清除快取
yarn cache clean
rm -rf node_modules yarn.lock
yarn install

# 解決方案 2：使用國內鏡像
yarn config set registry https://registry.npmmirror.com
yarn install
```

### 問題 2：`yarn start` 失敗（端口被占用）

```bash
# Windows
netstat -ano | findstr :4200
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:4200 | xargs kill -9

# 或使用其他端口
yarn ng serve --port 4201
```

### 問題 3：TypeScript 編譯錯誤

```bash
# 清除建置快取
yarn ng cache clean
rm -rf .angular

# 重新建置
yarn build
```

### 問題 4：Supabase 連線失敗

檢查 `src/environments/environment.ts`：
- ✅ `supabase.url` 是否正確
- ✅ `supabase.anonKey` 是否正確
- ✅ Supabase 專案是否在線

- --

## 📝 開發前檢查表

在開始新功能開發前，確認：

- [ ] 已拉取最新代碼：`git pull`
- [ ] 已切換到正確分支：`git checkout feature/xxx`
- [ ] 依賴已更新：`yarn install`
- [ ] Lint 無錯誤：`yarn lint`
- [ ] 開發伺服器可啟動：`yarn start`
- [ ] 已閱讀相關文檔
- [ ] 已了解要修改的模組結構

- --

## 🎯 開發時持續檢查

### 每次 Commit 前

```bash
# 1. Lint 檢查
yarn lint

# 2. 格式化程式碼（可選，pre-commit hook 會自動執行）
yarn prettier --write .

# 3. 本地測試
yarn test

# 4. Commit
git commit -m "feat: 新功能描述"
```

### 每次 Push 前

```bash
# 完整建置測試
yarn build

# 確認無建置錯誤後 Push
git push
```

- --

## 📞 取得協助

遇到問題時：

1. 查閱 [常見問題 FAQ](./29-常見問題-FAQ.md)
2. 查閱 [開發前檢查清單](./guides/pre-development-checklist.md)
3. 搜尋專案 Issue Tracker
4. 詢問團隊成員

- --

## 🔗 相關文檔

- [快速開始指南](./guides/getting-started.md) - 首次環境設定
- [開發最佳實踐指南](./guides/development-best-practices.md) - 代碼範例
- [開發前檢查清單](./guides/pre-development-checklist.md) - 完整檢查清單
- [SHARED_IMPORTS 使用指南](./reference/shared-imports-guide.md) - 必讀

- --

**最後更新**：2025-01-20
**維護者**：開發團隊

