# 狀態枚舉值定義

> **目的**: 定義 ng-alain-gighub 專案中所有狀態枚舉值，確保前後端狀態一致性和類型安全。

## 目標讀者 (Audience)

- 前端開發者
- 後端開發者
- AI Agents

## 📑 目錄

- [📋 目錄](#-目錄)
- [任務狀態](#任務狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [專案藍圖狀態](#專案藍圖狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [Pull Request 狀態](#pull-request-狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [協作者邀請狀態](#協作者邀請狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [協作者關係狀態](#協作者關係狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [待辦中心狀態](#待辦中心狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [待辦中心五種狀態分類](#待辦中心五種狀態分類)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [問題追蹤狀態](#問題追蹤狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [品質驗收狀態](#品質驗收狀態)
  - [定義](#定義)
    - [品質檢查狀態（quality_checks 表）](#品質檢查狀態quality_checks-表)
    - [驗收狀態（inspections 表）](#驗收狀態inspections-表)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [每日報表狀態](#每日報表狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [通知狀態](#通知狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [進度追蹤狀態](#進度追蹤狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [TypeScript 類型](#typescript-類型)
- [文件管理狀態](#文件管理狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [TypeScript 類型](#typescript-類型)
- [用戶會話狀態](#用戶會話狀態)
  - [定義](#定義)
  - [狀態流轉](#狀態流轉)
  - [TypeScript 類型](#typescript-類型)
- [優先級定義](#優先級定義)
  - [定義](#定義)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [嚴重程度定義](#嚴重程度定義)
  - [定義](#定義)
  - [資料庫定義](#資料庫定義)
  - [TypeScript 類型](#typescript-類型)
- [TypeScript 類型定義](#typescript-類型定義)
  - [統一導出](#統一導出)
- [📝 使用規範](#-使用規範)
  - [1. 單一真實來源](#1-單一真實來源)
  - [2. 資料庫遷移](#2-資料庫遷移)
  - [3. TypeScript 類型同步](#3-typescript-類型同步)
  - [4. 文檔同步](#4-文檔同步)
- [🔗 相關文檔](#-相關文檔)
- [📝 更新說明](#-更新說明)
  - [主要更新內容](#主要更新內容)

---


> 📋 **目的**：統一定義系統中所有狀態枚舉值，作為單一真實來源（Single Source of Truth）

**最後更新**：2025-11-15
**維護者**：開發團隊
**維護原則**：所有狀態定義變更必須同步更新本文檔

- --

- --

## 📋 目錄

- [任務狀態](#任務狀態)
- [專案藍圖狀態](#專案藍圖狀態)
- [Pull Request 狀態](#pull-request-狀態)
- [協作者邀請狀態](#協作者邀請狀態)
- [協作者關係狀態](#協作者關係狀態)
- [待辦中心狀態](#待辦中心狀態)
- [問題追蹤狀態](#問題追蹤狀態)
- [品質驗收狀態](#品質驗收狀態)
- [每日報表狀態](#每日報表狀態)
- [通知狀態](#通知狀態)
- [進度追蹤狀態](#進度追蹤狀態)
- [文件管理狀態](#文件管理狀態)
- [用戶會話狀態](#用戶會話狀態)
- [優先級定義](#優先級定義)
- [嚴重程度定義](#嚴重程度定義)
- [TypeScript 類型定義](#typescript-類型定義)

- --

## 任務狀態

### 定義

根據 `30-0-完整SQL表結構定義.md`，任務狀態定義如下：

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `pending` | Pending | 待處理 | 任務已建立但未開始 |
| `assigned` | Assigned | 已指派 | 任務已指派給負責人 |
| `in_progress` | In Progress | 進行中 | 任務正在執行 |
| `staging` | Staging | 暫存中 | 任務已提交，進入 48 小時暫存區 |
| `in_qa` | In QA | 品管中 | 任務進入品質檢查流程 |
| `in_inspection` | In Inspection | 驗收中 | 任務進入驗收流程 |
| `completed` | Completed | 已完成 | 任務已完成 |
| `cancelled` | Cancelled | 已取消 | 任務已取消 |

### 狀態流轉

```typescript
  ↓         ↓            ↓            ↓
cancelled ← cancelled ← cancelled ← cancelled
```

**注意**：根據 Git-like 分支模型和暫存區機制，任務提交後會進入 `staging` 狀態（48 小時可撤回），然後進入品管和驗收流程。

### 資料庫定義

根據 `30-0-完整SQL表結構定義.md`，`tasks` 表的狀態欄位定義：

```sql
status VARCHAR(20) DEFAULT 'pending' CHECK (
  status IN ('pending', 'assigned', 'in_progress', 'staging', 'in_qa', 'in_inspection', 'completed', 'cancelled')
)
```

### TypeScript 類型

```typescript
export type TaskStatus =
  | 'pending'
  | 'assigned'
  | 'in_progress'
  | 'staging'
  | 'in_qa'
  | 'in_inspection'
  | 'completed'
  | 'cancelled';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 任務狀態流轉圖
- [資料模型對照表](./34-資料模型對照表.md) - 任務模型定義
- [詞彙表](./reference/glossary.md) - 任務狀態說明

- --

## 專案藍圖狀態

### 定義

根據 `30-0-完整SQL表結構定義.md`，藍圖狀態定義如下：

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `planning` | Planning | 規劃中 | 專案規劃階段 |
| `active` | Active | 進行中 | 專案進行中 |
| `on_hold` | On Hold | 暫停 | 專案暫停（注意：使用 `on_hold` 而非 `paused`） |
| `completed` | Completed | 已完成 | 專案已完成 |
| `archived` | Archived | 已歸檔 | 專案已歸檔 |

### 狀態流轉

planning → active → completed → archived
```typescript
         on_hold
            ↓
         active
```

### 資料庫定義

根據 `30-0-完整SQL表結構定義.md`，`blueprints` 表的狀態欄位定義：

```sql
status VARCHAR(20) DEFAULT 'planning' CHECK (
  status IN ('planning', 'active', 'on_hold', 'completed', 'archived')
)
```

### TypeScript 類型

```typescript
export type BlueprintStatus =
  | 'planning'
  | 'active'
  | 'on_hold'
  | 'completed'
  | 'archived';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 專案藍圖狀態流轉圖
- [資料模型對照表](./34-資料模型對照表.md) - 藍圖模型定義
- [詞彙表](./reference/glossary.md) - 專案狀態說明

- --

## Pull Request 狀態

### 定義

根據 `22-完整SQL表結構定義.md`，Pull Request 狀態定義如下：

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `open` | Open | 打開 | PR 已建立，等待審查 |
| `reviewing` | Reviewing | 審核中 | PR 正在審查中 |
| `approved` | Approved | 已批准 | PR 已批准，等待合併 |
| `rejected` | Rejected | 已拒絕 | PR 已被拒絕 |
| `merged` | Merged | 已合併 | PR 已合併到主分支 |
| `closed` | Closed | 已關閉 | PR 已關閉 |

### 狀態流轉

open → reviewing → approved → merged
              ↓
```typescript
              ↓
          open (請求修改)
```

**注意**：根據 Git-like 分支模型，PR 合併時會調用 Edge Function `branch-merge` 更新 `tasks.contractor_fields`，並同步至主分支。

### 資料庫定義

根據 `22-完整SQL表結構定義.md`，`pull_requests` 表的狀態欄位定義：

```sql
status VARCHAR(20) DEFAULT 'open' CHECK (
  status IN ('open', 'reviewing', 'approved', 'rejected', 'merged', 'closed')
)
```

### TypeScript 類型

```typescript
export type PRStatus =
  | 'open'
  | 'reviewing'
  | 'approved'
  | 'rejected'
  | 'merged'
  | 'closed';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - Pull Request 狀態流轉圖
- [資料模型對照表](./34-資料模型對照表.md) - PR 模型定義
- [業務流程圖](./04-業務流程圖.mermaid.md) - PR 審查流程

- --

## 協作者邀請狀態

### 定義

根據 `22-完整SQL表結構定義.md`，協作者邀請狀態定義如下：

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `pending` | Pending | 待處理 | 邀請已發送，等待回應 |
| `accepted` | Accepted | 已接受 | 邀請已被接受 |
| `rejected` | Rejected | 已拒絕 | 邀請已被拒絕 |
| `expired` | Expired | 已過期 | 邀請已過期 |

### 狀態流轉

pending → accepted (建立協作關係)
       ↓
    rejected (邀請終止)
```typescript
    expired (邀請失效)
```

**注意**：邀請接受後會建立協作關係（`organization_collaborations`），並啟用分支。

### 資料庫定義

根據 `22-完整SQL表結構定義.md`，`collaboration_invitations` 表的狀態欄位定義：

```sql
status VARCHAR(20) DEFAULT 'pending' CHECK (
  status IN ('pending', 'accepted', 'rejected', 'expired')
)
```

### TypeScript 類型

```typescript
export type InvitationStatus =
  | 'pending'
  | 'accepted'
  | 'rejected'
  | 'expired';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 協作者邀請狀態流轉圖
- [資料模型對照表](./34-資料模型對照表.md) - 協作邀請模型定義
- [業務流程圖](./04-業務流程圖.mermaid.md) - 協作邀請流程

- --

## 協作者關係狀態

### 定義

根據 `22-完整SQL表結構定義.md`，協作者關係狀態定義如下：

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `pending` | Pending | 待處理 | 協作關係已建立，等待啟用 |
| `active` | Active | 活躍 | 協作關係活躍中 |
| `suspended` | Suspended | 已暫停 | 協作關係已暫停 |
| `ended` | Ended | 已結束 | 協作關係已結束 |

### 狀態流轉

pending → active → suspended → active (恢復)
                  ↓
               ended (協作終止)

```typescript
```

**注意**：協作關係支援多種協作類型（contractor, subcontractor, consultant, partner），並可暫停和恢復。

### 資料庫定義

根據 `22-完整SQL表結構定義.md`，`organization_collaborations` 表的狀態欄位定義：

```sql
status VARCHAR(20) DEFAULT 'active' CHECK (
  status IN ('pending', 'active', 'suspended', 'ended')
)
```

### TypeScript 類型

```typescript
export type CollaborationStatus =
  | 'pending'
  | 'active'
  | 'suspended'
  | 'ended';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 協作者關係狀態流轉圖
- [資料模型對照表](./34-資料模型對照表.md) - 協作關係模型定義
- [業務流程圖](./04-業務流程圖.mermaid.md) - 協作關係管理流程

- --

## 待辦中心狀態

### 定義

根據 `22-完整SQL表結構定義.md`，待辦中心狀態定義如下：

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `pending` | Pending | 待執行 | 待辦事項待執行（🟦） |
| `in_progress` | In Progress | 進行中 | 待辦事項進行中 |
| `staging` | Staging | 暫存中 | 待辦事項暫存中（🟨，48 小時可撤回） |
| `in_qa` | In QA | 品管中 | 待辦事項品管中（🟧） |
| `in_inspection` | In Inspection | 驗收中 | 待辦事項驗收中（🟥） |
| `completed` | Completed | 已完成 | 待辦事項已完成 |
| `cancelled` | Cancelled | 已取消 | 待辦事項已取消 |

### 狀態流轉

pending → in_progress → staging → in_qa → in_inspection → completed
  ↓           ↓            ↓
cancelled ← cancelled ← cancelled
```typescript
```

**注意**：待辦中心聚合多種來源的待辦事項（tasks, issues, review_pr, qa_check, inspection），狀態與來源實體同步。

### 待辦中心五種狀態分類

根據 `27-完整架構流程圖.mermaid.md` 和 `22-完整SQL表結構定義.md`，待辦中心（`personal_todos` 表）包含五種狀態分類：

- 🟦 **待執行**（`pending`）- 來自 `tasks`
- 🟨 **暫存中**（`staging`）- 來自 `task_staging`（48 小時可撤回）
- 🟧 **品管中**（`in_qa`）- 來自 `quality_checks`
- 🟥 **驗收中**（`in_inspection`）- 來自 `inspections`
- ⚠️ **問題追蹤**（`open`, `in_progress`）- 來自 `issues`

### 資料庫定義

根據 `22-完整SQL表結構定義.md`，`personal_todos` 表的狀態欄位定義：

```sql
status VARCHAR(20) DEFAULT 'pending' CHECK (
  status IN ('pending', 'in_progress', 'staging', 'in_qa', 'in_inspection', 'completed', 'cancelled')
)
```

### TypeScript 類型

```typescript
export type TodoStatus =
  | 'pending'
  | 'in_progress'
  | 'staging'
  | 'in_qa'
  | 'in_inspection'
  | 'completed'
  | 'cancelled';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 待辦中心狀態流轉圖
- [資料模型對照表](./34-資料模型對照表.md) - 待辦中心模型定義
- [業務流程圖](./04-業務流程圖.mermaid.md) - 待辦中心流程

- --

## 問題追蹤狀態

### 定義

根據 `30-0-完整SQL表結構定義.md`，問題狀態定義如下：

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `open` | Open | 開啟 | 問題已開立（注意：使用 `open` 而非 `new`） |
| `in_progress` | In Progress | 處理中 | 問題正在處理 |
| `resolved` | Resolved | 已解決 | 問題已解決 |
| `closed` | Closed | 已關閉 | 問題已關閉 |
| `wont_fix` | Won't Fix | 不修復 | 問題不會修復 |

### 狀態流轉

open → in_progress → resolved → closed
                      ↓
                   wont_fix → closed
```typescript
```

**注意**：根據 Git-like 分支模型，所有分支問題會即時同步至主分支（`issue_sync_logs` 表）。

### 資料庫定義

根據 `30-0-完整SQL表結構定義.md`，`issues` 表的狀態欄位定義：

```sql
status VARCHAR(20) DEFAULT 'open' CHECK (
  status IN ('open', 'in_progress', 'resolved', 'closed', 'wont_fix')
)
```

### TypeScript 類型

```typescript
export type IssueStatus =
  | 'open'
  | 'in_progress'
  | 'resolved'
  | 'closed'
  | 'wont_fix';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 問題追蹤狀態流轉圖
- [資料模型對照表](./34-資料模型對照表.md) - 問題模型定義
- [實體關係圖](./12-實體關係圖.mermaid.md) - 問題表結構

- --

## 品質驗收狀態

### 定義

根據 `30-0-完整SQL表結構定義.md`，品質檢查和驗收狀態定義如下：

#### 品質檢查狀態（quality_checks 表）

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `pending` | Pending | 待檢查 | 等待品質檢查 |
| `in_progress` | In Progress | 檢查中 | 正在執行品質檢查 |
| `passed` | Passed | 通過 | 品質檢查通過 |
| `failed` | Failed | 不通過 | 品質檢查不通過 |
| `conditional_pass` | Conditional Pass | 條件通過 | 品質檢查條件通過 |

#### 驗收狀態（inspections 表）

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `pending` | Pending | 待驗收 | 等待驗收 |
| `in_progress` | In Progress | 驗收中 | 正在執行驗收 |
| `accepted` | Accepted | 已接受 | 驗收通過（責任轉移） |
| `rejected` | Rejected | 已拒絕 | 驗收不通過 |
| `conditional_accept` | Conditional Accept | 條件接受 | 驗收條件接受 |

### 狀態流轉

**品質檢查流程**：
pending → in_progress → passed → [進入驗收]
                      ↓
                   failed
```mermaid
              conditional_pass
```

**驗收流程**（責任切割）：
pending → in_progress → accepted (責任轉移 = TRUE)
                      ↓
                   rejected
                      ↓
```typescript
```

### 資料庫定義

根據 `30-0-完整SQL表結構定義.md`：

**quality_checks 表**：
```sql
status VARCHAR(20) DEFAULT 'pending' CHECK (
  status IN ('pending', 'in_progress', 'passed', 'failed', 'conditional_pass')
)
```

**inspections 表**：
```sql
status VARCHAR(20) DEFAULT 'pending' CHECK (
  status IN ('pending', 'in_progress', 'accepted', 'rejected', 'conditional_accept')
)
```

### TypeScript 類型

```typescript
export type QualityCheckStatus =
  | 'pending'
  | 'in_progress'
  | 'passed'
  | 'failed'
  | 'conditional_pass';

export type InspectionStatus =
  | 'pending'
  | 'in_progress'
  | 'accepted'
  | 'rejected'
  | 'conditional_accept';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 品質驗收狀態流轉圖
- [實體關係圖](./12-實體關係圖.mermaid.md) - 品質驗收表結構

- --

## 每日報表狀態

### 定義

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `draft` | Draft | 草稿 | 報表草稿 |
| `submitted` | Submitted | 已提交 | 報表已提交 |
| `reviewed` | Reviewed | 已審核 | 報表已審核 |

### 狀態流轉

draft → submitted → reviewed




```typescript
```

### 資料庫定義

```sql
CREATE TYPE daily_report_status AS ENUM (
  'draft',
  'submitted',
  'reviewed'
);
```

### TypeScript 類型

```typescript
export type DailyReportStatus =
  | 'draft'
  | 'submitted'
  | 'reviewed';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 每日報表狀態流轉圖
- [實體關係圖](./12-實體關係圖.mermaid.md) - 每日報表表結構

- --

## 通知狀態

### 定義

根據 `30-0-完整SQL表結構定義.md`，通知狀態使用布林值 `is_read` 而非枚舉：

| 欄位 | 類型 | 說明 |
|------|------|------|
| `is_read` | `boolean` | 是否已讀（`false` = 未讀，`true` = 已讀） |
| `read_at` | `timestamptz` | 已讀時間（可選） |

### 狀態流轉

未讀 (is_read = false) → 已讀 (is_read = true, read_at = NOW())
```typescript
```

### 資料庫定義

根據 `30-0-完整SQL表結構定義.md`，`notifications` 表的定義：

```sql
is_read BOOLEAN DEFAULT FALSE,
read_at TIMESTAMPTZ,
```

### TypeScript 類型

```typescript
export interface Notification {
  id: string;
  recipientId: string;
  // ... 其他欄位
  isRead: boolean;
  readAt: string | null;
}
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 通知狀態流轉圖
- [實體關係圖](./12-實體關係圖.mermaid.md) - 通知表結構

- --

## 進度追蹤狀態

### 定義

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `initialized` | Initialized | 已初始化 | 進度已初始化 |
| `updating` | Updating | 更新中 | 進度更新中 |
| `complete` | Complete | 已完成 | 進度 100% 完成 |

### 狀態流轉

initialized → updating → complete
```typescript
            updating (循環更新)
```

### TypeScript 類型

```typescript
export type ProgressTrackingStatus =
  | 'initialized'
  | 'updating'
  | 'complete';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 進度追蹤狀態流轉圖
- [實體關係圖](./12-實體關係圖.mermaid.md) - 進度追蹤表結構

- --

## 文件管理狀態

### 定義

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `uploading` | Uploading | 上傳中 | 文件上傳中 |
| `active` | Active | 可用 | 文件可用 |
| `archived` | Archived | 已歸檔 | 文件已歸檔 |

### 狀態流轉

uploading → active → archived

```typescript
```

### TypeScript 類型

```typescript
export type DocumentStatus =
  | 'uploading'
  | 'active'
  | 'archived';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 文件管理狀態流轉圖
- [實體關係圖](./12-實體關係圖.mermaid.md) - 文件表結構

- --

## 用戶會話狀態

### 定義

| 狀態值 | 英文名稱 | 中文名稱 | 說明 |
|--------|---------|---------|------|
| `authenticated` | Authenticated | 已登入 | 用戶已登入 |
| `active` | Active | 活躍中 | 用戶活躍中 |
| `idle` | Idle | 閒置中 | 用戶閒置中 |
| `expired` | Expired | 已過期 | Session 已過期 |

### 狀態流轉

authenticated → active → idle → expired
```typescript
                └────────┘
```

### TypeScript 類型

```typescript
export type SessionStatus =
  | 'authenticated'
  | 'active'
  | 'idle'
  | 'expired';
```

**參考文檔**：
- [狀態圖](./14-狀態圖.mermaid.md) - 用戶會話狀態流轉圖

- --

## 優先級定義

### 定義

| 優先級值 | 英文名稱 | 中文名稱 | 說明 |
|---------|---------|---------|------|
| `low` | Low | 低 | 低優先級 |
| `medium` | Medium | 中 | 中等優先級 |
| `high` | High | 高 | 高優先級 |
| `urgent` | Urgent | 緊急 | 緊急優先級 |

### 資料庫定義

```sql
CREATE TYPE priority_level AS ENUM (
  'low',
  'medium',
  'high',
  'urgent'
);
```

### TypeScript 類型

```typescript
export type PriorityLevel =
  | 'low'
  | 'medium'
  | 'high'
  | 'urgent';
```

**參考文檔**：
- [詞彙表](./reference/glossary.md) - 優先級說明
- [資料模型對照表](./34-資料模型對照表.md) - 任務模型定義

- --

## 嚴重程度定義

### 定義

| 嚴重程度值 | 英文名稱 | 中文名稱 | 說明 |
|-----------|---------|---------|------|
| `low` | Low | 低 | 低嚴重程度 |
| `medium` | Medium | 中 | 中等嚴重程度 |
| `high` | High | 高 | 高嚴重程度 |
| `critical` | Critical | 緊急 | 緊急嚴重程度 |

### 資料庫定義

```sql
CREATE TYPE severity_level AS ENUM (
  'low',
  'medium',
  'high',
  'critical'
);
```

### TypeScript 類型

```typescript
export type SeverityLevel =
  | 'low'
  | 'medium'
  | 'high'
  | 'critical';
```

**參考文檔**：
- [實體關係圖](./12-實體關係圖.mermaid.md) - 問題表結構

- --

## TypeScript 類型定義

### 統一導出

建議在 `src/app/shared/models/status.enum.ts` 中統一導出所有狀態類型：

```typescript
/**
 * 狀態枚舉值統一定義
 *
 * 參考文檔：docs/43-狀態枚舉值定義.md
 */

// 任務狀態
export type TaskStatus =
  | 'pending'
  | 'assigned'
  | 'in_progress'
  | 'staging'
  | 'in_qa'
  | 'in_inspection'
  | 'completed'
  | 'cancelled';

// 專案藍圖狀態
export type BlueprintStatus =
  | 'planning'
  | 'active'
  | 'on_hold'
  | 'completed'
  | 'archived';

// Pull Request 狀態
export type PRStatus =
  | 'open'
  | 'reviewing'
  | 'approved'
  | 'rejected'
  | 'merged'
  | 'closed';

// 協作者邀請狀態
export type InvitationStatus =
  | 'pending'
  | 'accepted'
  | 'rejected'
  | 'expired';

// 協作者關係狀態
export type CollaborationStatus =
  | 'pending'
  | 'active'
  | 'suspended'
  | 'ended';

// 待辦中心狀態
export type TodoStatus =
  | 'pending'
  | 'in_progress'
  | 'staging'
  | 'in_qa'
  | 'in_inspection'
  | 'completed'
  | 'cancelled';

// 問題追蹤狀態
export type IssueStatus =
  | 'open'
  | 'in_progress'
  | 'resolved'
  | 'closed'
  | 'wont_fix';

// 品質驗收狀態
export type QualityCheckStatus =
  | 'pending'
  | 'in_progress'
  | 'passed'
  | 'failed'
  | 'conditional_pass';

// 每日報表狀態
export type DailyReportStatus =
  | 'draft'
  | 'submitted'
  | 'reviewed';

// 通知狀態（使用布林值 is_read，此處僅供參考）
// export type NotificationStatus = 'unread' | 'read';

// 進度追蹤狀態
export type ProgressTrackingStatus =
  | 'initialized'
  | 'updating'
  | 'complete';

// 文件管理狀態
export type DocumentStatus =
  | 'uploading'
  | 'active'
  | 'archived';

// 用戶會話狀態
export type SessionStatus =
  | 'authenticated'
  | 'active'
  | 'idle'
  | 'expired';

// 優先級
export type PriorityLevel =
  | 'low'
  | 'medium'
  | 'high'
  | 'urgent';

// 嚴重程度
export type SeverityLevel =
  | 'low'
  | 'medium'
  | 'high'
  | 'critical';
```

- --

## 📝 使用規範

### 1. 單一真實來源

- **本文檔**是所有狀態定義的單一真實來源
- 所有狀態變更必須先更新本文檔
- 其他文檔應引用本文檔，而非重複定義

### 2. 資料庫遷移

- 新增或修改狀態時，必須建立資料庫遷移腳本
- 遷移腳本應包含 `CREATE TYPE` 或 `ALTER TYPE` 語句
- 參考：`supabase/migrations/`

### 3. TypeScript 類型同步

- 狀態定義變更後，必須同步更新 TypeScript 類型
- 建議使用 `src/app/shared/models/status.enum.ts` 統一管理
- 使用 TypeScript 嚴格模式確保類型安全

### 4. 文檔同步

- 狀態定義變更後，必須同步更新以下文檔：
  - `14-狀態圖.mermaid.md` - 狀態流轉圖
  - `34-資料模型對照表.md` - 資料模型定義
  - `42-詞彙表.md` - 術語說明

- --

## 🔗 相關文檔

- [狀態圖](./14-狀態圖.mermaid.md) - 狀態流轉視覺化
- [資料模型對照表](./34-資料模型對照表.md) - 資料庫與 TypeScript 對照
- [詞彙表](./reference/glossary.md) - 術語說明
- [實體關係圖](./12-實體關係圖.mermaid.md) - 資料庫表結構
- [業務流程圖](./04-業務流程圖.mermaid.md) - 業務流程視覺化
- [文檔一致性分析報告](./44-文檔一致性分析報告.md) - 文檔分析報告

- --

- --

## 📝 更新說明

本文檔已根據 `30-0-完整SQL表結構定義.md` 的完整 SQL 表結構定義進行更新，確保狀態定義與資料庫結構完全一致。

### 主要更新內容

1. **任務狀態**：新增 `staging`、`in_qa`、`in_inspection` 狀態，反映暫存區和驗收流程
2. **藍圖狀態**：使用 `on_hold` 而非 `paused`
3. **Pull Request 狀態**：新增完整 PR 狀態定義（open, reviewing, approved, rejected, merged, closed）
4. **協作者邀請狀態**：新增協作邀請狀態定義（pending, accepted, rejected, expired）
5. **協作者關係狀態**：新增協作關係狀態定義（pending, active, suspended, ended）
6. **待辦中心狀態**：新增待辦中心狀態定義，包含五種狀態分類
7. **問題狀態**：使用 `open` 而非 `new`，新增 `wont_fix` 狀態
8. **品質驗收狀態**：區分品質檢查狀態和驗收狀態，新增 `conditional_pass` 和 `conditional_accept`
9. **通知狀態**：改為使用布林值 `is_read` 而非枚舉
10. **文檔引用更新**：統一更新狀態圖文檔引用為 `14-狀態圖.mermaid.md`

- --

**最後更新**：2025-01-15
**維護者**：開發團隊
**對齊版本**：22-完整SQL表結構定義.md v2.0（51 張資料表）
**狀態圖版本**：14-狀態圖.mermaid.md v2.0（補充 PR、協作、待辦狀態機）
**下次審查**：2025-02-15

